# ===================================================================
# FASTAPI BACKEND - VARIÁVEIS DE AMBIENTE
# ===================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# ===================================================================

# Secret para autenticação entre serviços (Next.js e FastAPI)
# Deve ser EXATAMENTE igual ao FASTAPI_SECRET no Next.js
# Gere um secret seguro: openssl rand -hex 32
FASTAPI_SECRET=seu-secret-super-seguro-aqui-123456

# Porta do servidor (Railway/Render definem automaticamente via $PORT)
PORT=8000

# URL do site Next.js (para CORS)
# Desenvolvimento: http://localhost:3000
# Produção: https://orbeelabs.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OpenAI API Key (Opcional)
# Apenas se quiser usar IA para geração de conteúdo
# Se não configurado, usa templates pré-definidos
OPENAI_API_KEY=sk-...
